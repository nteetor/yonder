% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-form.R
\name{input_form}
\alias{input_form}
\alias{form_submit_button}
\alias{update_form}
\alias{submit_form}
\title{Form inputs}
\usage{
input_form(id, ..., label = NULL, layout = NULL)

form_submit_button(label, ..., value = label)

update_form(id, label = NULL, session = get_current_session())

submit_form(id, value, session = get_current_session())
}
\arguments{
\item{id}{A character string. The id of the reactive input.}

\item{...}{Reactive inputs.}

\item{label}{A character string. The label of the input.}

\item{layout}{A character string.}

\item{value}{A character string. The input's value when the submit button is
clicked.}

\item{session}{A shiny session object.}
}
\description{
Form inputs are a new reactive input. Form inputs are an alternative to
\link[shiny:submitButton]{shiny::submitButton}. A form input is comprised of any number of inputs. The
values of these inputs do not reactively update. The inputs will reactively
update when a form submit button is clicked.

A form input's reactive value depends on the clicked form submit button. This
allows server logic to distinguish between different form submission types,
think "login" versus "register".
}
\details{
\subsection{Submits that wait}{

Usually a submit is immediate: the held values are released and the
form's own value is set, in that order, as the button is clicked.

A nested input may have work that must finish first, though — an
\code{\link[=input_file]{input_file()}} with \code{upload_mode = "manual"} uploads its staged files
at this point, and its value is set by the server when that upload
ends. The submit then waits for it. Every submit button in the form
is disabled and the clicked one shows a pending state, so a form that
is waiting does not read as a form that is broken. The values go once
the work finishes, which is what lets an observer keyed on the
submit read the uploaded files.

If that work fails, or the user cancels it, nothing is sent at all.
The held values stay held, so submitting again retries with
everything intact.
}
}
\seealso{
Other inputs:
\code{\link[=floating_label]{floating_label()}},
\code{\link[=input_button]{input_button()}},
\code{\link[=input_checkbox]{input_checkbox()}},
\code{\link[=input_checkbox_group]{input_checkbox_group()}},
\code{\link[=input_chip_group]{input_chip_group()}},
\code{\link[=input_file]{input_file()}},
\code{\link[=input_list_group]{input_list_group()}},
\code{\link[=input_menu]{input_menu()}},
\code{\link[=input_multi_select]{input_multi_select()}},
\code{\link[=input_numeric]{input_numeric()}},
\code{\link[=input_radio_group]{input_radio_group()}},
\code{\link[=input_range]{input_range()}},
\code{\link[=input_select]{input_select()}},
\code{\link[=input_text]{input_text()}},
\code{\link[=input_text_group]{input_text_group()}}
}
\concept{inputs}
