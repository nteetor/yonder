% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-file.R
\name{update_file}
\alias{update_file}
\title{Update a file input}
\usage{
update_file(
  id,
  ...,
  accept = NULL,
  placeholder = NULL,
  summary = NULL,
  enable = NULL,
  disable = NULL,
  session = get_current_session()
)
}
\arguments{
\item{id}{A character string. The id of the reactive input.}

\item{...}{These dots are for future extensions and must be empty.}

\item{accept}{A character vector. The new bound on what may be
chosen, as file extensions (\code{".csv"}), MIME types (\code{"text/csv"}),
or MIME wildcards (\code{"image/*"}).}

\item{placeholder}{A character string. The new prompt shown inside
the drop zone.}

\item{summary}{A character string. The new template for the file
list's summary line; see \code{\link[=input_file]{input_file()}} for the tokens.}

\item{enable}{If \code{TRUE}, enable the input.}

\item{disable}{If \code{TRUE}, disable the input. When both \code{enable} and
\code{disable} are \code{TRUE}, \code{disable} wins.}

\item{session}{A shiny session object.}
}
\value{
No return value, called for side effects.
}
\description{
Update the properties of an \code{\link[=input_file]{input_file()}}: what it accepts, its
prompt and summary line, and its enabled state. The value is never
set from the server — the upload protocol writes it as a batch
completes, and offers no way to set or unset it.
}
\seealso{
\code{\link[=input_file]{input_file()}}, \code{\link[=reset_file]{reset_file()}}
}
