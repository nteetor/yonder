% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-file.R
\name{input_file}
\alias{input_file}
\alias{update_file}
\title{File input}
\usage{
input_file(
  id,
  ...,
  label = NULL,
  select = c("one", "many"),
  accept = NULL,
  capture = NULL,
  placeholder = NULL
)

update_file(
  id,
  ...,
  reset = NULL,
  accept = NULL,
  placeholder = NULL,
  enable = NULL,
  disable = NULL,
  session = get_current_session()
)
}
\arguments{
\item{id}{A character string. The id of the reactive input.}

\item{...}{Optional named arguments specifying HTML attributes for the input
element.}

\item{label}{A character string. The label of the input, rendered as
the legend of the fieldset wrapping the input.}

\item{select}{A character string. With \code{"one"} (default) a single file
may be chosen, with \code{"many"} any number.}

\item{accept}{A character vector of file extensions (\code{".csv"}), MIME
types (\code{"text/csv"}), or MIME wildcards (\code{"image/*"}) bounding what
may be chosen, defaults to \code{NULL}, in which case any file may be
chosen.}

\item{capture}{A character string, one of \code{"user"} or \code{"environment"},
asking a mobile browser to capture a new photo or video with the
front or rear camera instead of browsing existing files. Defaults to
\code{NULL}, browse only.}

\item{placeholder}{A character string. The prompt shown inside the drop
zone, defaults to \code{"Choose a file"}.}

\item{reset}{If \code{TRUE}, clear the file list, progress, and any error,
defaults to \code{NULL}. A batch in flight is cancelled. The server value
is left as it is — the upload protocol has no way to unset it.}

\item{enable}{If \code{TRUE}, enable the input, defaults to \code{NULL}.}

\item{disable}{If \code{TRUE}, disable the input, defaults to \code{NULL}. When
both \code{enable} and \code{disable} are \code{TRUE}, \code{disable} wins.}

\item{session}{A shiny session object.}
}
\value{
A shiny tag object.
}
\description{
Upload files to the server. Files upload as soon as they are chosen —
over HTTP, beside the WebSocket — so the app stays responsive while
bytes are in transit. A progress bar tracks the batch and a cancel
control abandons it.
}
\details{
\subsection{Server value}{

A data frame with one row per uploaded file and the columns \code{name},
\code{size}, \code{type}, and \code{datapath} — the same contract as
\code{\link[shiny:fileInput]{shiny::fileInput()}}, because the server side of the upload protocol
produces it. \code{datapath} points at a temporary file owned by the
session; copy anything you need to keep.

The value is \code{NULL} until the first upload completes. Re-rendering the
input (through \code{\link[shiny:renderUI]{shiny::renderUI()}}, say) resets it to \code{NULL}, again as
with \code{\link[shiny:fileInput]{shiny::fileInput()}}.
}

\subsection{Upload size}{

Shiny caps uploads at \code{getOption("shiny.maxRequestSize")}, 5 MB by
default, and rejects a batch if any one file exceeds it. Raise it for
larger uploads:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(shiny.maxRequestSize = 30 * 1024^2)
}\if{html}{\out{</div>}}

The limit is read at render time to pre-validate on the client, and
enforced again by the server on every upload.
}

\subsection{Inside a form}{

\code{\link[=input_form]{input_form()}} holds an input's value back until the form is
submitted. A file input is not held back: its value is set by the
server when the upload finishes, not by a client-side input change.
}
}
\seealso{
\code{\link[=update_file]{update_file()}}

Other inputs:
\code{\link[=floating_label]{floating_label()}},
\code{\link[=input_button]{input_button()}},
\code{\link[=input_checkbox]{input_checkbox()}},
\code{\link[=input_checkbox_group]{input_checkbox_group()}},
\code{\link[=input_chip_group]{input_chip_group()}},
\code{\link[=input_form]{input_form()}},
\code{\link[=input_list_group]{input_list_group()}},
\code{\link[=input_menu]{input_menu()}},
\code{\link[=input_multi_select]{input_multi_select()}},
\code{\link[=input_numeric]{input_numeric()}},
\code{\link[=input_radio_group]{input_radio_group()}},
\code{\link[=input_range]{input_range()}},
\code{\link[=input_select]{input_select()}},
\code{\link[=input_text]{input_text()}},
\code{\link[=input_text_group]{input_text_group()}}
}
\concept{inputs}
