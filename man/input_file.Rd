% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-file.R
\name{input_file}
\alias{input_file}
\title{File input}
\usage{
input_file(
  id,
  ...,
  label = NULL,
  select = c("one", "many"),
  upload_mode = c("auto", "manual"),
  upload_button = c("show", "none"),
  upload_max = NULL,
  accept = NULL,
  capture = NULL,
  placeholder = NULL,
  summary = NULL,
  height = NULL
)
}
\arguments{
\item{id}{A character string. The id of the reactive input.}

\item{...}{Optional named arguments specifying HTML attributes for the input
element.}

\item{label}{A character string. The label of the input, rendered as
the legend of the fieldset wrapping the input.}

\item{select}{A character string. With \code{"one"} (default) a single file
may be chosen, with \code{"many"} any number.}

\item{upload_mode}{A character string. With \code{"auto"} (default) files
upload as soon as they are chosen. With \code{"manual"} files accumulate
in a staged set — each removable before flight, a same-named
addition replacing its predecessor — and an explicit action starts
the batch: the input's Upload button, a surrounding
\code{\link[=input_form]{input_form()}}'s submit, or \code{\link[=file_upload_start]{file_upload_start()}} from the server.
A cancelled or failed batch returns to the staged set, ready to
retry whole.}

\item{upload_button}{A character string. With \code{"show"} (default) a
manual-mode input renders its own Upload button; with \code{"none"} it
does not, leaving the batch to an app-supplied trigger —
\code{\link[=file_upload_start]{file_upload_start()}}, or a surrounding \code{\link[=input_form]{input_form()}}, whose
submit already starts it. With the button hidden and neither of
those in place, nothing visible starts the batch. The cancel
control still appears while a batch is in flight. Ignored in auto
mode, which has no Upload button.}

\item{upload_max}{A whole number bounding how many files one batch
may contain, defaults to \code{NULL}, no bound.

In manual mode, the staged set counts toward the bound and the input stops
accepting files once full — remove a file to make room. Delivered rows do
not count: they are not part of the next batch. A file with the same name
as a staged one replaces it rather than adding, so a replacing drop passes
even at the bound.

In auto mode, and for drops and pastes that would overfill a staged set, a
gesture selecting too many files is rejected whole rather than trimmed —
quietly keeping part of a selection would read as data loss. With \code{select = "one"} a single file is already the bound.}

\item{accept}{A character vector of file extensions (\code{".csv"}), MIME
types (\code{"text/csv"}), or MIME wildcards (\code{"image/*"}) bounding what
may be chosen, defaults to \code{NULL}, in which case any file may be
chosen.}

\item{capture}{A character string, one of \code{"user"} or \code{"environment"},
asking a mobile browser to capture a new photo or video with the
front or rear camera instead of browsing existing files. Defaults to
\code{NULL}, browse only.}

\item{placeholder}{A character string. The prompt shown inside the drop
zone, defaults to \code{"Choose a file"}.}

\item{summary}{A character string, a template for the file list's
summary line, defaults to \code{NULL}, in which case
\code{"{files} · {size}"} applies. Tokens are replaced from upload state:
\itemize{
\item \code{{files}}, the pluralized file count, "3 files".
\item \code{{n}}, the bare file count, "3".
\item \code{{size}}, the total size of the batch, "2.0 MB".
\item \code{{done}}, the number of files uploaded so far.
\item \code{{failed}}, the number of files that failed.
\item \code{{percent}}, the batch progress as a whole number, "63".
}

Unknown tokens are left as-is. The summary stays visible while the
file list is collapsed, so the state tokens can make it a compact
status line, e.g. \code{"{done}/{n} uploaded"}.}

\item{height}{A character string, a CSS length setting the minimum
height of the drop zone, defaults to \code{NULL}, in which case the drop
zone is twice the height of a standard input. Any CSS length works:
\code{"12rem"}, \code{"200px"}, \code{"clamp(6rem, 20vh, 16rem)"}. A theme may set
the default for every file input through the
\code{--bsides-file-dropzone-height} CSS variable; \code{height} overrides the
theme for its input.}
}
\value{
A shiny tag object.
}
\description{
Upload files to the server. By default files upload as soon as they
are chosen; with \code{upload_mode = "manual"} they accumulate in a staged
set, each removable, until an explicit action starts the batch. A
progress bar tracks a batch in flight and a cancel control abandons
it.
}
\details{
\subsection{Uploading}{

Uploaded bytes travel over HTTP rather than the app's websocket
connection, so the session stays responsive while an upload is in
flight — a long upload does not block reactivity, and the R process
does work only when a batch begins and ends.
}

\subsection{Server value}{

A data frame with one row per uploaded file and the columns \code{name},
\code{size}, \code{type}, and \code{datapath} — the same contract as
\code{\link[shiny:fileInput]{shiny::fileInput()}}, because the server side of the upload protocol
produces it. \code{datapath} points at a temporary file owned by the
session; copy anything you need to keep.

The value is \code{NULL} until the first upload completes. Re-rendering the
input (through \code{\link[shiny:renderUI]{shiny::renderUI()}}, say) resets it to \code{NULL}, again as
with \code{\link[shiny:fileInput]{shiny::fileInput()}}.
}

\subsection{Upload size}{

Shiny caps uploads at \code{getOption("shiny.maxRequestSize")}, 5 MB by
default, and rejects a batch if any one file exceeds it. Raise it for
larger uploads:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(shiny.maxRequestSize = 30 * 1024^2)
}\if{html}{\out{</div>}}

The limit is read at render time to pre-validate on the client, and
enforced again by the server on every upload.
}

\subsection{Inside a form}{

\code{\link[=input_form]{input_form()}} holds an input's value back until the form is
submitted. An auto-mode file input is not held back: its value is set
by the server when the upload finishes, not by a client-side input
change, so it lands as soon as the upload does — before the form is
submitted, not with it.

\code{upload_mode = "manual"} is the answer inside a form. Files stage
while the form is filled in, the batch starts when the form submits,
and the form holds its own value back until that upload finishes. So
an observer keyed on the submit sees the uploaded files:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(input$form_id, \{
  files <- input$file_id
\})
}\if{html}{\out{</div>}}

The submit button shows a pending state while it waits; an app that
wants its own indicator can read \code{\link[=file_upload_progress]{file_upload_progress()}}. If the
upload fails, or the user cancels it, the form is not submitted at
all — nothing is sent, the staged files are kept, and submitting
again retries them.
}

\subsection{Known limitations}{

Bookmarking saves an uploaded file, but restoring a bookmark does not
re-populate the input. Whole directories cannot be uploaded: the
protocol carries bare file names, so the paths within a directory
would not survive.
}
}
\seealso{
\code{\link[=update_file]{update_file()}}, \code{\link[=reset_file]{reset_file()}}, \code{\link[=file_upload_start]{file_upload_start()}}

Other inputs:
\code{\link[=floating_label]{floating_label()}},
\code{\link[=input_button]{input_button()}},
\code{\link[=input_checkbox]{input_checkbox()}},
\code{\link[=input_checkbox_group]{input_checkbox_group()}},
\code{\link[=input_chip_group]{input_chip_group()}},
\code{\link[=input_form]{input_form()}},
\code{\link[=input_list_group]{input_list_group()}},
\code{\link[=input_menu]{input_menu()}},
\code{\link[=input_multi_select]{input_multi_select()}},
\code{\link[=input_numeric]{input_numeric()}},
\code{\link[=input_radio_group]{input_radio_group()}},
\code{\link[=input_range]{input_range()}},
\code{\link[=input_select]{input_select()}},
\code{\link[=input_text]{input_text()}},
\code{\link[=input_text_group]{input_text_group()}}
}
\concept{inputs}
