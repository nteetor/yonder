% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-chip-group.R
\name{input_chip_group}
\alias{input_chip_group}
\alias{update_chip_group}
\title{Chip group input}
\usage{
input_chip_group(
  id,
  ...,
  choices = NULL,
  values = choices,
  select = values,
  type = "primary",
  layout = c("vertical", "horizontal")
)

update_chip_group(
  id,
  choices = NULL,
  values = choices,
  select = NULL,
  enable = NULL,
  disable = NULL,
  session = get_current_session()
)
}
\arguments{
\item{id}{A character string. The id of the reactive input.}

\item{...}{Optional named arguments specifying HTML attributes for the input
element.}

\item{choices}{A character vector of chip labels, defaults to \code{NULL}.
When \code{NULL}, the input renders without chips and may be populated
later with \code{\link[=update_chip_group]{update_chip_group()}}.}

\item{values}{A character vector of values for the input's choices,
defaults to \code{choices}.}

\item{select}{A character vector naming the checked values, one or more
of \code{values}, defaults to \code{values} — every chip starts checked. Pass
\code{NULL} to render all chips unchecked. In \code{\link[=update_chip_group]{update_chip_group()}},
\code{select} defaults to \code{NULL}, leaving the checked set untouched;
otherwise \code{select} replaces the checked set — the current selection
is reset and the new selection applied.}

\item{type}{A character string, a Bootstrap theme color name. Checked
chips have a solid \code{type} background; unchecked chips have a \code{type}
border. Defaults to \code{"primary"}.}

\item{layout}{A character string. With \code{"vertical"} (default) chips wrap
onto new lines; with \code{"horizontal"} chips stay on one scrolling row.}

\item{enable}{If \code{TRUE}, enable the input, defaults to \code{NULL}.}

\item{disable}{If \code{TRUE}, disable the input, defaults to \code{NULL}. When both
\code{enable} and \code{disable} are \code{TRUE}, \code{disable} wins.}

\item{session}{A shiny session object.}
}
\value{
A \link[htmltools:tag]{htmltools::tag} object.
}
\description{
A reactive input whose choices render as toggleable chips — a checkbox
group rendered as chips. Clicking a chip (or pressing Enter or Space on
it) toggles its checked state; the input's value is the set of checked
chips. The set of chips is fixed: to let users add and remove values,
see \code{\link[=input_multi_select]{input_multi_select()}}.
}
\details{
\subsection{Server value}{

A character vector of the checked chips' values, or \code{NULL} when no chips
are checked.
}

When \code{update_chip_group()} replaces an input's choices, checked values
whose chip no longer exists fall out of the checked set (pass \code{select}
in the same call to choose the new selection). Without \code{values} in the
call, \code{select} cannot be validated server-side; unmatched values are
dropped with a warning in the browser console.
}
\seealso{
Other inputs:
\code{\link[=fileInput]{fileInput()}},
\code{\link[=input_button]{input_button()}},
\code{\link[=input_checkbox]{input_checkbox()}},
\code{\link[=input_checkbox_group]{input_checkbox_group()}},
\code{\link[=input_form]{input_form()}},
\code{\link[=input_list_group]{input_list_group()}},
\code{\link[=input_menu]{input_menu()}},
\code{\link[=input_multi_select]{input_multi_select()}},
\code{\link[=input_numeric]{input_numeric()}},
\code{\link[=input_radio_group]{input_radio_group()}},
\code{\link[=input_range]{input_range()}},
\code{\link[=input_select]{input_select()}},
\code{\link[=input_text]{input_text()}},
\code{\link[=input_text_group]{input_text_group()}}
}
\concept{inputs}
